Script to manage data of an Oracle NoSQL database, community edition.

Requisites:
1. Oracle NoSQL community edition, version 1.2.123, up & running.
2. Jython 2.5.2.
Both Oracle NoSQL and Jython are installed on the SAME machine.

Tested on a CentOS 5.7 virtual machine.

Usage:
1. Modify the first lines of the file jython_oraclenosql.py to reflect
   your location of Oracle's jar files, kvstore-xxx.jar and je.jar.
2. On a command prompt, type
   /absolute/path/jython -i /absolute/path/jython_oraclenosql.py
3. Assuming above created Jython's console and 
   preloaded jython_oraclenosql.py, you can type:

   connect("oracle_store_name","host:port")
   countAll()
   version()
   
   put("Key1/Key2/-/MinorKey1","Value")
   get("Key1/Key2/-/MinorKey1")
   delete("Key1/Key2/-/MinorKey1")
   multiDelete("Key1")
   storeIterator ("Key1")

   


